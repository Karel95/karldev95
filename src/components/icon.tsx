const Icon = () => {
  return (
    <img
      className="logo fixed-top"
      src="images/profile.png"
      alt="Foto de Karel Hernández"
      style={{ borderRadius: '50%' }}
    />
  );
};

export default Icon;
